<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="Tabs">
        <menu>
            <item
                android:id="@+id/vision"
                android:icon="@drawable/ic_menu_send"
                android:title="Vision" />
            <item
                android:id="@+id/swamiji"
                android:icon="@drawable/ic_menu_send"
                android:title="Sri Swamiji" />
            <item
                android:id="@+id/program"
                android:icon="@drawable/ic_menu_send"
                android:title="Bhagavad Gita Mahayagna program " />
            <item
                android:id="@+id/journey"
                android:icon="@drawable/ic_menu_send"
                android:title="The Journey" />
            <item
                android:id="@+id/timeline"
                android:icon="@drawable/ic_menu_send"
                android:title="Timeline of Events" />
            <item
                android:id="@+id/upcoming"
                android:icon="@drawable/ic_menu_send"
                android:title="Upcoming Events" />
            <item
                android:id="@+id/tutor"
                android:icon="@drawable/ic_menu_send"
                android:title="Tutor Application " />
        </menu>
    </item>

</menu>
